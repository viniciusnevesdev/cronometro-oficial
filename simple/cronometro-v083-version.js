/* v0.8.3 — correções visuais e de layout. */
/* APP_RELEASE é a versão exibida no pacote gerado. Estes metadados de schema
   continuam históricos e não são alterados pela publicação. */
globalThis.APP_META=Object.freeze({version:/^\d+\.\d+\.\d+(?:-[\w.]+)?$/.test(String(globalThis.APP_RELEASE||''))?globalThis.APP_RELEASE:'0.8.3',dataSchemaVersion:5,factoryDataVersion:1});
